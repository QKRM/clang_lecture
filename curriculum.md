# C언어 강의 전체 커리큘럼 (16차시)

## 강의 개요

| 항목 | 내용 |
|------|------|
| 총 차시 | 16차시 |
| 차시당 시간 | 3시간 |
| 강사 설명 | 1시간 30분 |
| 학생 자습/구현 | 1시간 30분 |
| 개발 환경 | GitHub Codespaces |
| 최종 목표 | libsbs 스타일로 표준 C 라이브러리 함수 직접 구현 |

## 강의 목표

1. C언어 기초 문법을 체계적으로 이해한다.
2. 포인터와 메모리 구조를 명확히 파악한다.
3. 표준 C 라이브러리 함수의 동작 원리를 이해하고 직접 구현할 수 있다.
4. Makefile을 사용해 라이브러리를 빌드하는 방법을 익힌다.

---

## 차시별 커리큘럼

### 1차시 - GitHub Codespaces 환경 세팅 + C언어 첫걸음

- **학습 목표**
  - GitHub Codespaces를 설정하고 C언어 개발 환경을 구축할 수 있다.
  - Hello World 프로그램을 작성하고 컴파일, 실행할 수 있다.
  - C언어 소스 파일의 기본 구조를 설명할 수 있다.
- **강사 설명**: Codespaces 생성, gcc 사용법, 컴파일 과정, main 함수 구조, printf 기초
- **실습**: Codespaces 직접 세팅 + Hello World 작성 및 변형 실습

---

### 2차시 - 변수, 자료형, 연산자

- **학습 목표**
  - C언어의 기본 자료형(int, char, float, double)을 구분하고 사용할 수 있다.
  - 변수를 선언하고 초기화할 수 있다.
  - 산술, 비교, 논리 연산자를 올바르게 사용할 수 있다.
- **강사 설명**: 자료형 크기와 범위, 변수 선언/초기화, 형 변환, 연산자 우선순위
- **실습**: 간단한 계산기 프로그램 작성, sizeof 실험

---

### 3차시 - 조건문과 반복문

- **학습 목표**
  - if/else, switch 문으로 조건 분기를 구현할 수 있다.
  - for, while, do-while 반복문을 상황에 맞게 사용할 수 있다.
  - break, continue로 흐름을 제어할 수 있다.
- **강사 설명**: 조건문 문법과 활용, 반복문 종류와 차이점, 중첩 반복문
- **실습**: 구구단 출력, FizzBuzz, 숫자 맞추기 게임

---

### 4차시 - 함수와 재귀

- **학습 목표**
  - 함수를 선언, 정의, 호출할 수 있다.
  - 값에 의한 전달(pass by value)을 이해한다.
  - 재귀 함수의 동작 원리를 설명하고 구현할 수 있다.
- **강사 설명**: 함수 프로토타입, 매개변수와 반환값, 스택 프레임, 재귀 개념
- **실습**: 팩토리얼, 피보나치 수열 구현 (반복문 버전 vs 재귀 버전)

---

### 5차시 - 포인터 기초

- **학습 목표**
  - 포인터의 개념과 메모리 주소를 이해한다.
  - `&` 주소 연산자와 `*` 역참조 연산자를 사용할 수 있다.
  - 포인터를 통해 값을 읽고 수정할 수 있다.
- **강사 설명**: 메모리 구조, 주소 개념, 포인터 선언과 초기화, 포인터 연산
- **실습**: swap 함수 구현 (포인터 버전), 포인터 값 추적 실습

---

### 6차시 - 배열과 포인터

- **학습 목표**
  - 배열과 포인터의 관계를 설명할 수 있다.
  - 포인터 산술 연산을 배열 순회에 활용할 수 있다.
  - 2차원 배열과 포인터 배열을 사용할 수 있다.
- **강사 설명**: 배열 이름과 포인터, 포인터 산술, 배열의 메모리 배치
- **실습**: 정수 배열 정렬(버블 정렬), 포인터로 배열 순회

---

### 7차시 - 문자열과 포인터

- **학습 목표**
  - C 문자열의 구조(null terminator)를 이해한다.
  - 문자열을 포인터로 조작할 수 있다.
  - 문자 배열과 문자 포인터의 차이를 설명할 수 있다.
- **강사 설명**: null terminator, 문자열 리터럴, char 배열 vs char 포인터, 문자열 순회
- **실습**: 문자열 뒤집기, 대소문자 변환, 문자 개수 세기

---

### 8차시 - 구조체와 typedef

- **학습 목표**
  - 구조체를 선언하고 멤버에 접근할 수 있다.
  - typedef로 타입 별칭을 정의할 수 있다.
  - 구조체 포인터를 사용할 수 있다.
- **강사 설명**: struct 선언, 멤버 접근(. 와 ->), typedef, 구조체 배열
- **실습**: 학생 정보 관리 프로그램 (구조체 배열 활용)

---

### 9차시 - 헤더 파일 + 문자 분류 함수 구현 (is*, toupper, tolower)

- **학습 목표**
  - 헤더 파일(`.h`)의 역할과 선언/정의 분리를 설명할 수 있다.
  - `#include "libsbs.h"`로 직접 만든 헤더를 포함하고, `<>`와 `""`의 차이를 안다.
  - 인클루드 가드(`#ifndef / #define / #endif`)로 중복 포함을 막을 수 있다.
  - ASCII 코드 체계를 이해하고 문자 분류 기준을 설명할 수 있다.
  - `sbs_isalpha`, `sbs_isdigit`, `sbs_isalnum`, `sbs_isascii`, `sbs_isprint`를 구현할 수 있다.
  - `sbs_toupper`, `sbs_tolower`를 구현할 수 있다.
- **강사 설명**: 헤더 파일 개념(선언 vs 정의), `#include "..."` vs `<...>`, 인클루드 가드, 함수 프로토타입을 헤더에 모으기 / ASCII 테이블, 문자 범위 개념, 각 함수의 명세 분석
- **실습**: `libsbs.h` 작성 + 7개 함수 직접 구현 + 채점 스크립트로 자동 테스트

---

### 10차시 - 메모리 함수 구현 I (memset, bzero, memcpy, memccpy)

- **학습 목표**
  - void 포인터(void*)의 개념과 용도를 이해한다.
  - `sbs_memset`, `sbs_bzero`를 구현할 수 있다.
  - `sbs_memcpy`, `sbs_memccpy`를 구현할 수 있다.
- **강사 설명**: void* 캐스팅, 바이트 단위 복사 원리, 함수 명세 분석
- **실습**: 4개 함수 직접 구현 + 테스트

---

### 11차시 - 메모리 함수 구현 II (memmove, memchr, memcmp, calloc)

- **학습 목표**
  - `memmove`와 `memcpy`의 차이(메모리 겹침 처리)를 설명할 수 있다.
  - `sbs_memmove`, `sbs_memchr`, `sbs_memcmp`를 구현할 수 있다.
  - 동적 메모리 할당(`malloc`, `free`)을 이해하고 `sbs_calloc`을 구현할 수 있다.
- **강사 설명**: 메모리 겹침 문제, malloc/free, 동적 메모리 관리
- **실습**: 4개 함수 직접 구현 + 메모리 누수 체크

---

### 12차시 - 문자열 함수 구현 I (strlen, strlcpy, strlcat)

- **학습 목표**
  - `sbs_strlen`을 구현하고 null terminator 처리를 이해한다.
  - `sbs_strlcpy`와 `sbs_strlcat`의 버퍼 안전 처리 방식을 이해하고 구현한다.
- **강사 설명**: 문자열 길이 계산, 버퍼 오버플로우 위험, strlcpy vs strcpy 차이
- **실습**: 3개 함수 직접 구현 + 버퍼 안전성 테스트

---

### 13차시 - 문자열 함수 구현 II (strchr, strrchr, strnstr)

- **학습 목표**
  - `sbs_strchr`, `sbs_strrchr`로 문자 검색을 구현할 수 있다.
  - `sbs_strnstr`로 부분 문자열 검색을 구현할 수 있다.
- **강사 설명**: 검색 알고리즘 개념, 순방향/역방향 탐색, 부분 문자열 매칭
- **실습**: 3개 함수 직접 구현 + 엣지 케이스 테스트 + 도전 과제(단어 검색기, 파일 경로 분리기, 파일에서 내용 찾기(open/read), 심화 함수 `sbs_strcasestr`)

---

### 14차시 - 문자열 함수 구현 III (strncmp, atoi, strdup)

- **학습 목표**
  - `sbs_strncmp`로 문자열 비교를 구현할 수 있다.
  - `sbs_atoi`로 문자열을 정수로 변환하는 로직을 구현할 수 있다.
  - `sbs_strdup`으로 동적 메모리를 활용한 문자열 복제를 구현할 수 있다.
- **강사 설명**: 문자열 비교 원리, 숫자 파싱 로직, malloc을 활용한 복제
- **실습**: 3개 함수 직접 구현 + atoi 엣지 케이스(음수, 공백, 오버플로우) + 도전 과제(버전 비교기, 커맨드라인 계산기, 심화 함수 `sbs_atol`)

---

### 15차시 - Makefile과 라이브러리 아카이브

- **학습 목표**
  - Makefile의 기본 문법과 규칙을 이해한다.
  - `ar` 명령어로 정적 라이브러리(.a 파일)를 생성할 수 있다.
  - libsbs.a를 빌드하고 다른 프로그램에 링크할 수 있다.
- **강사 설명**: Makefile 문법, 타겟/의존성/레시피, ar 명령어, 링킹 개념
- **실습**: libsbs.a Makefile 작성 + 빌드 + 링크 테스트

---

### 16차시 - libsbs 종합 프로젝트 + 코드 리뷰

- **학습 목표**
  - 지금까지 구현한 모든 함수를 하나의 libsbs 라이브러리로 통합한다.
  - 각 함수의 헤더 파일(libsbs.h)을 작성한다.
  - 동료 코드 리뷰를 통해 코드 품질을 개선한다.
- **강사 설명**: 헤더 파일 작성 방법, 프로젝트 구조 정리, 코드 리뷰 방법론
- **실습**: libsbs 전체 통합 + 상호 코드 리뷰 + 최종 빌드 확인

---

## 구현 함수 전체 목록

### 문자 분류 함수 (9차시)
| 함수 | 설명 |
|------|------|
| `sbs_isalpha` | 알파벳 문자 여부 확인 |
| `sbs_isdigit` | 숫자 문자 여부 확인 |
| `sbs_isalnum` | 알파벳 또는 숫자 여부 확인 |
| `sbs_isascii` | ASCII 범위(0-127) 여부 확인 |
| `sbs_isprint` | 출력 가능 문자 여부 확인 |
| `sbs_toupper` | 소문자를 대문자로 변환 |
| `sbs_tolower` | 대문자를 소문자로 변환 |

### 메모리 함수 (10-11차시)
| 함수 | 설명 |
|------|------|
| `sbs_memset` | 메모리 블록을 특정 값으로 초기화 |
| `sbs_bzero` | 메모리 블록을 0으로 초기화 |
| `sbs_memcpy` | 메모리 블록 복사 |
| `sbs_memccpy` | 특정 문자 발견 시까지 메모리 복사 |
| `sbs_memmove` | 겹치는 메모리 영역 안전 복사 |
| `sbs_memchr` | 메모리 블록에서 문자 검색 |
| `sbs_memcmp` | 두 메모리 블록 비교 |
| `sbs_calloc` | 0으로 초기화된 동적 메모리 할당 |

### 문자열 함수 (12-14차시)
| 함수 | 설명 |
|------|------|
| `sbs_strlen` | 문자열 길이 계산 |
| `sbs_strlcpy` | 버퍼 안전 문자열 복사 |
| `sbs_strlcat` | 버퍼 안전 문자열 이어붙이기 |
| `sbs_strchr` | 문자열에서 문자 순방향 검색 |
| `sbs_strrchr` | 문자열에서 문자 역방향 검색 |
| `sbs_strnstr` | 문자열에서 부분 문자열 검색 |
| `sbs_strncmp` | 두 문자열 비교 |
| `sbs_atoi` | 문자열을 정수로 변환 |
| `sbs_strdup` | 동적 메모리로 문자열 복제 |
| `sbs_strcasestr` (심화, 13차시) | 대소문자 무시 부분 문자열 검색 |
| `sbs_atol` (심화, 14차시) | 문자열을 큰 정수(long)로 변환 |
